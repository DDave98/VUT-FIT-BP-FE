import './LoginForm.css';
import React from 'react';

const LoginForm = () => 
{

    return <>
        <form className='props' >
            {/*props*/}
        </form>
    </>
}

LoginForm.propTypes = {}

export default withRouter(LoginForm);
